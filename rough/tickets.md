<table><thead><tr><th>#</th><th>the requirement</th><th>what it forces</th></tr></thead><tbody><tr><td>1</td><td>"I want to see what the <em>policy</em> sees, next to the wide view"</td><td>compositing — and you hit 256 vs 128 not stacking</td></tr><tr><td>2</td><td><em>do #1 with <code>PIL.Image.resize</code> first, look hard at the result, then with <code>np.repeat</code></em></td><td><strong>why nearest-neighbour</strong>, as something you saw</td></tr><tr><td>3</td><td>"three seeds in one video"</td><td>the loop body has to become a function</td></tr><tr><td>4</td><td>"which seed failed, and why?"</td><td>returning <code>(frames, placed, reason)</code> instead of printing inline</td></tr><tr><td>5</td><td>"try frontview without editing task.yaml"</td><td>argparse — and <code>replace()</code>, because <code>TaskConfig</code> is frozen</td></tr><tr><td>6</td><td>"run it twice in one process"</td><td><code>try/finally: env.close()</code></td></tr><tr><td>7</td><td>"the placement flashes past"</td><td>hold frames</td></tr><tr><td>8</td><td>"that <code>fps=30</code> — where should it come from?"</td><td><code>env.control_hz</code>, and the argument for why any other value lies</td></tr><tr><td>9</td><td>"<code>core/eval.py</code> will need the panel too, and it must never import robosuite"</td><td>the split into <code>core/video.py</code></td></tr></tbody></table>